<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="CustomDialog" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item><!--边框-->
        <item name="android:windowIsFloating">true</item><!--是否浮现在activity之上-->
        <item name="android:windowIsTranslucent">false</item><!--半透明-->
        <item name="android:windowNoTitle">true</item><!--无标题-->
        <item name="android:windowBackground">@android:color/transparent</item><!--背景透明-->
        <item name="android:backgroundDimEnabled">true</item><!--模糊-->
    </style>

    <style name="dialog_test_title">
        <item name="android:textColor">#ff4500</item>
    </style>

    <!-- 带各种颜色的进度条 -->
    <declare-styleable name="ProgressColors">
        <!-- 阴影的宽度 -->
        <attr name="shadowWidth" format="dimension"/>
        <!-- 阴影的颜色 -->
        <attr name="shadowColor" format="color"/>
        <!-- 进度条的宽度 -->
        <attr name="progressWidth" format="dimension"/>
    </declare-styleable>
    
    <!-- 横向的选择器 -->
    <declare-styleable name="HorizontalPickerView">
        <!-- 选中的颜色 -->
        <attr name="selectColor" format="color"/>
        <!-- item之间的间隔 -->
        <attr name="itemInterval" format="dimension"/>
        <!-- item的字体大小 -->
        <attr name="txtSize" format="dimension"/>
    </declare-styleable>

</resources>
